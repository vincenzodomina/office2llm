services:
  office2llm:
    build:
      context: .
    volumes:
      - ./data:/data
    # Run like:
    #   docker compose run --rm office2llm --input /data/in.docx --outdir /data/out --dpi 200
    entrypoint: ["office2llm"]
